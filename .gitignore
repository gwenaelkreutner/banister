__pycache__/
*.py[cod]
venv/
.pytest_cache/
.env
# Wiring Docker spécifique à un déploiement (ex. réseau partagé avec un Phoenix
# self-hosté pour le tracing LLM — voir CLAUDE.md § Observabilité) : jamais générique
# entre self-hosters, docker-compose.yml doit rester autonome sans ce fichier.
docker-compose.override.yml
*.egg-info/
eval/results

# Runtime data (spec 001 FR-004): the athlete's own SQLite database and instance lock —
# never belongs in the repo, and data/ now holds real personal training data from live
# testing during this session.
data/
.scratch_migration/